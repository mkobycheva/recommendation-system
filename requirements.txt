pandas==2.2.2
numpy>=2.0
fsspec==2025.3.0
requests==2.32.4

# scikit-learn, scipy, implicit, gensim, torch, torchvision, torchaudio,
# lightning, pytorch-lightning, torchmetrics, wandb are intentionally NOT
# pinned/listed here: this notebook only needs pandas/numpy/torch/sklearn's
# LabelEncoder — implicit/gensim/scipy aren't used at all, and pinning
# scikit-learn fights Colab's own bundled packages (umap-learn, hdbscan,
# tsfresh, access all want scikit-learn>=1.6/scipy>=1.14 already).
